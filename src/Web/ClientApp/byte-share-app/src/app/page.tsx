/**
 * Home component serves as the entry point for the home page of the application.
 * It renders the HomePage component inside a div, providing the main content for the home route.
 */

import HomePage from './home/page'



export default function Home() {

  return ( 
    <div>
        <HomePage/>
    </div>
  );
}
